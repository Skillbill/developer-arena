To enable these hooks just link them to the git hooks directory.

For instance, to enable the pre-commit script, from the project root run:

```
ln -rsf ./scripts/git-hooks/pre-commit.sh .git/hooks/pre-commit
```
